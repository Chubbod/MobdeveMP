<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ph.edu.dlsu.s12.chuajohn.finalproject.sudoku">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sudoku">
        <activity android:name=".GameHistoryActivity"></activity>
        <activity android:name=".ThemeActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".StartActivity" />
        <activity android:name=".GameMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".game.GameActivity" />
    </application>

</manifest>